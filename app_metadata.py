"""Single source of truth for desktop release metadata."""
from __future__ import annotations


APPLICATION_NAME = "StimTrace"
# Bump for every distributable release. Cloud-worker compatibility remains
# independently versioned because notebook updates may be required between app releases.
APPLICATION_VERSION = "1.0.2"
